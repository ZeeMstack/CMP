# Open Questions

Unresolved matters requiring an explicit decision before the relevant work is built. Per `CLAUDE.md`, do not invent agronomic, quality, security, or architectural rules — record the gap here instead.

None of the items below are treated as invented values; they are recorded as open rather than answered.

## Technical decisions

- ~~**OIDC provider selection.**~~ Resolved: Auth0 is the implemented production identity provider, behind the OIDC-compatible application adapter approved here (`docs/domain/AUTHORIZATION_MODEL.md`, "Layered trust model").
- **PostgreSQL RLS policy detail.** RLS is approved as defence in depth alongside mandatory application-level tenant scoping, but concrete policy definitions (per-table policies, role setup) are not yet specified. Does not block application scaffolding.
- **Frontend effective-permission signal.** No endpoint currently exposes a caller's effective permission set to the frontend — every screen renders its actions unconditionally and relies on the backend's `403` to reject an unauthorized attempt. A proper permission-aware action UX (hiding/disabling actions a role cannot perform, rather than rendering and failing) is a cross-product future concern affecting every module, not a Store & Inventory-specific gap. `UX-IA-001` deliberately continues relying on existing backend authorization and does not add a Store-only workaround; do not invent a client-side role→permission mapping in the meantime.

## Operational greenhouse decisions

- Exact set of controlled location templates beyond nursery/leafy/vine examples in `CMP_MASTER_SPEC.md` §3 (e.g. additional greenhouse types) is not yet defined.
- Sanitation/release requirement definitions per location type (referenced in spec §3.4) are not yet specified.

## Agronomic decisions

- Crop/variety-specific stage definitions, expected durations, and harvest modes are explicitly deferred to versioned crop/workflow configuration (spec §8) and are not to be invented ahead of that configuration work.
- Quality/QC thresholds and release criteria are not yet defined.

## Store and inventory decisions

- ~~**InventoryLot↔SeedLot cardinality.**~~ Resolved by the `STORE-INV-002A` discovery: `SeedLot.inventory_lot_id` (nullable FK, not the reverse direction) — one tenant-wide `InventoryLot` may link to many Farm-scoped `SeedLot`s, at most one per Farm. See `docs/domain/STORE_INVENTORY_MODEL.md` §15.

## Deferred commercial decisions

- Customer specification structure and versioning details (spec §8) beyond "customer specifications are versioned" are not yet defined.
- Recall process detail beyond the traceability genealogy requirement (spec §10) is not yet defined.

## Vines Production decisions

- **Vines Grow Cube disposition correction: replace-mode deferred.** `VINES-OPS-002` implements void-only correction (`production_disposition_service.correct_grow_cube_disposition`) for a Vines `grow_bag` disposition REDUCTION -- a pure reversal that restores every one of the target event's own named Grow Cube(s) to disposal eligibility, delegating unmodified to the already carrier-agnostic `correct_disposition`. REPLACE-mode correction (voiding an original loss and simultaneously recording a corrected replacement REDUCTION) is NOT exposed for Vines: `correct_disposition`'s existing REPLACE shape carries only a bare `plant_loss_count`, with no field for which specific Grow Cube(s) the replacement names, and fabricating that identity (e.g. reusing the original event's own Grow Cubes) would be an invented fact, not a derived one, when the correction's whole point may be that the ORIGINAL identification was wrong. Extending `ProductionDispositionEvent`'s REPLACE shape to carry an explicit corrected Grow Cube set is left for a follow-up ticket rather than rushed here (per the ticket's own instruction: "if extending correction materially complicates this ticket, STOP ONLY that subpart"). Void-then-re-record (two separate, already-supported commands) is the current operator path for a full "I identified the wrong plant" correction.

## Crop Observations operator UI decisions

- **No farm/tenant-wide observation feed.** `AGRONOMY-OPS-001` built the Observations workspace (`/farms/{farmId}/observations`) strictly against the existing backend surface: `GET .../crop-batches/{batch_id}/observations` is per-Batch only, with no farm- or tenant-wide "recent observations across every batch" read. The ticket's own compact mockup showed a cross-batch log, but the ticket also scoped the allowed backend change to "one small read endpoint... for usable UI target selection" — a farm-wide observation feed is a materially larger read (aggregation, pagination, filtering) past that allowance, not the target-selection gap it was meant for. The shipped UI instead requires selecting a Batch first, then shows that Batch's own observation log — operationally reasonable (a grower reviewing "this batch's" history), but not the cross-batch feed literally pictured. Adding a real farm-wide feed is a separate, larger backend ticket.
- **No "Recorded by" display name anywhere in this app yet.** `ObservationEventRead.actor_user_id` is a raw UUID with no resolution to a display name in any existing frontend code path — every other operational history panel already built (Plant Loss History, Vines Loss History, Harvest History) likewise omits "recorded by" from its UI for the same reason: surfacing the raw UUID would violate the "no UUIDs in normal UI" rule (`CLAUDE.md`), and there is no `/users` list or actor-name-resolution endpoint to call instead. The Observation History table omits this column, consistent with existing precedent, rather than inventing a lookup. A general actor-name-resolution capability (e.g. a `GET /users?ids=` batch lookup, or embedding a display name summary directly in relevant `*Read` schemas) would benefit every history view in the app, not just Observations, and is left as a cross-cutting follow-up rather than a one-off fix here.
- **Carrier target location resolution covers direct Location occupancy only.** The new `GET .../crop-batches/{batch_id}/observation-targets` read (added by this ticket) resolves a carrier's current location only when its active `Occupancy` targets a `Location` directly; a carrier occupying an `asset_position` (e.g. a Nursery Tray resting on a specific Trolley level) shows no location in the target picker rather than walking through the asset to its own location. This mirrors `production_disposition_service.list_active_production_plates`'s own established scope (same limitation, same precedent) rather than introducing new asset-position-aware location-resolution logic; the UI degrades gracefully (shows the carrier code with no location) rather than fabricating one.

## Planning decisions

- **No yield/duration/density assumptions existed to reuse.** Before building `PLANNING-OPS-001`, the repo was checked for any already-configured crop cycle-duration, expected-yield, plant-density, or germination-rate data (`Crop`, `Variety`, `Workflow`, `WorkflowStage` models/migrations) — none exists. Per the ticket's own explicit instruction, none was invented: the planner enters `required_quantity` (demand), `planned_quantity` (seed/plant count), and `expected_coverage_quantity` (the planner's own honest estimate of what that sowing will cover) all by hand; CMP performs no yield derivation. Automatic optimization is future work, not scoped here.
- **`expected_coverage_uom_id` is forced to exactly equal the parent Production Requirement's own `quantity_uom_id`.** A plan line's coverage and the requirement's demand are summed directly with no conversion step. The alternative — allowing a different UOM and silently converting via `uom_conversion_service` — was rejected: `EA`/`SEED`↔mass conversion has no real-world factor to convert with (a seed count cannot be converted to a harvested kg estimate without exactly the yield assumption the ticket forbids inventing), and even a valid mass↔mass conversion would quietly blur "the planner's own coverage estimate" into "a system-computed estimate," which is a bigger judgment call than this ticket's scope. Locking the UOM keeps every coverage number an honest, unconverted, planner-entered figure.
- **Production Requirement `status` has no `reopen` command.** OPEN → CLOSED and (from OPEN or CLOSED) → CANCELLED are implemented; the ticket's own status vocabulary section describes only these three states with CLOSED/CANCELLED read as effectively terminal for planning purposes, and no reopen flow was described in the compact UX. If a planner needs to resume planning against a closed requirement, the current path is creating a new Production Requirement. Left as a genuine gap rather than invented.
- **Update/close/cancel commands are idempotent by final state, not by full command-replay dedup.** Mirrors `LocationUpdate`/`LocationDeactivate`/`LocationReactivate`'s own established per-command `<verb>_client_command_id`/`<verb>_request_fingerprint` column-pair pattern exactly (see `docs/domain` — UX-IA-001). A second, different `client_command_id` cancelling an already-cancelled row is treated as a harmless no-op (same target state) rather than raising a conflict, which is a deliberate, existing precedent in this codebase (`location_service.deactivate_location`), not a Planning-specific shortcut.
- **Seeding Program Line `crop_id` must exactly equal its parent Production Requirement's `crop_id`; `variety_id` is independent (optional, unchecked against the requirement's own variety).** A plan line's whole purpose is fulfilling ITS requirement's demand, so a crop mismatch is rejected outright as nonsensical; variety substitution (planting a different but acceptable variety of the same crop to meet the same demand) is a real planning scenario the ticket's own example data doesn't rule out, so it is allowed.

## UI brand system decisions

- **Quality hold severity tone: Caution (existing) vs. Critical (as literally listed in the PILOT-UI-002 approved palette).** The approved GrowCMP status system's own example list places "hold" under Critical ("hold, recall, blocked, rejected, serious exceptions"), while every existing quality-hold surface in the app today renders it as Caution/amber: `StatusBadge`'s `attention` tone (`components/StatusBadge.tsx`, comment "Awaiting QC / hold"), `QualityHoldBanner.tsx` (hand-rolled `amber-*` classes, not yet on any `wl-*` token), and every call site passing `tone="attention"` for an open hold (e.g. `app/farms/[farmId]/crop-batches/[batchId]/page.tsx`, `app/farms/[farmId]/setup-readiness/page.tsx`). Reclassifying "hold" from Caution to Critical is a quality-severity signaling decision (how alarming an open hold should look, tenant-wide), not a pure visual-token remap, so PILOT-UI-002 left every existing hold/attention call site unchanged and added a `critical` tone to `StatusBadge` (mapped to the already-approved `wl-flag-bg`/`wl-flag-fg`) as an additive option only, consumed by no caller yet. Recoloring quality holds (and deciding which other existing "attention" uses, if any, should move to Critical instead) needs an explicit product/quality decision, not an inferred one.
- **Raw Tailwind status colors not yet migrated to `wl-grow`/`wl-hold`/`wl-flag` tokens.** A repo-wide scan found roughly 50 components (mostly per-screen forms, history panels, and domain-specific badges such as `QualityHoldBanner.tsx`, `processing/RecallBadge.tsx`, and various `*Form.tsx` validation-error styling) still using hardcoded `red-*`/`amber-*`/`green-*` Tailwind utility classes instead of the semantic `wl-flag`/`wl-hold`/`wl-grow` tokens PILOT-UI-002 established as canonical. PILOT-UI-002 was scoped to the shared token system, app shell, and common primitives (`Button`, `Tabs`, table recipe, `PageHeader`, `Breadcrumbs`, `StatusBadge`, `EmptyState`, `ErrorState`, `LoadingSkeleton`, `NetworkStatusIndicator`) and explicitly excluded rewriting individual screens; migrating this remaining 50-file tail to semantic tokens (and resolving the Caution-vs-Critical question above along the way, since several of them are hold/recall-shaped) is left as a follow-up sweep rather than done ad hoc here.
- **Legacy `/admin` platform-tenant console still on the pre-Waterline `brand-*`/`ink`/`surface` token set.** `components/admin/AdminShell.tsx` (and its three `/admin/tenants*` routes) was never migrated to the `wl-*` Waterline tokens in the prior PILOT-UX-001A2-R2 batch either, and is not one of the "operator" modules PILOT-UI-002's ticket enumerates (Farm Home, Nursery, Production, Store & Inventory, Processing, etc. — all farm-operator screens). Left untouched; migrating it is a separate decision since it is platform-admin surface, not farm-operator surface, and would need its own token-mapping pass (it isn't reachable by remapping `wl-*`, since it doesn't use those tokens at all).

## Farm work engine decisions (PILOT-OPS-001)

- **No reopen command for a `COMPLETED`/`CANCELLED` Farm Work Item.** The ticket's own status vocabulary lists exactly `OPEN`/`IN_PROGRESS`/`BLOCKED`/`COMPLETED`/`CANCELLED` with both terminal states read as final, and no reopen flow was described. If an operator needs to resume work on a completed/cancelled item, the current path is creating a new Work Item. Left as a genuine gap rather than invented, mirroring `PRODUCTION_REQUIREMENT`'s own identical precedent (see "Planning decisions" above).
- ~~**Manual Work Item creation has no location/asset/carrier/batch picker yet.**~~ Resolved by the PILOT-OPS-001 closure pass: the Create form's "Add context" disclosure adds all four, each reusing an already-fetched farm-scoped read (Locations tree flattened via the existing `flattenLocationTree` helper, the Batch summary, Assets, Carriers) — see `docs/domain/FARM_WORK_ITEM_MODEL.md`, "Manual Work Item structured context".
- ~~**Leafy/Vines Harvest recording pages are not wired to `work_item_id`.**~~ Resolved for Leafy by the PILOT-OPS-001 closure pass: the real operator-facing `POST /farms/{farm_id}/leafy-production/harvests` route now accepts and links `work_item_id`, and the Leafy Harvest page carries Work Item context through end-to-end (see `docs/domain/FARM_WORK_ITEM_MODEL.md`, "Leafy/Vines Harvest linkage"). Vines is wired on the backend identically but has **no frontend "Open Harvest" link yet** — a Farm Work Item carries no crop-classification signal to route Leafy vs. Vines automatically, and guessing would be worse than not linking; left open until a Work Item (or its Batch) exposes a resolvable crop/production-system classification.

## Migration graph decisions

- ~~**`seed_lots.inventory_lot_id` vs. pre-STORE-INV-004 downgrade-guard tests.**~~ Resolved by PILOT-BLOCKER-006 (F40): the four named tests were each given a version-specific raw-SQL `seed_lots` insert (the exact `id, tenant_id, farm_id, crop_id, variety_id, code, supplier_name, supplier_lot_reference, received_date, expiry_date, status, created_by_user_id` column set that existed at `d17a4e2f9c86`, before `inventory_lot_id` was added by `f1a4c8e7b2d5`), mirroring the same established pattern each file already used for every other schema-evolved table at its own deliberately-downgraded revision. No production model, service, or migration changed; only the four test fixtures. `VINES-OPS-001B` adds the first Alembic merge revision (`475ed950cb4e`) joining the previously separate, never-connected Nursery/InterSalads branch (`b7e2f4a9c1d6`) and the STORE-INV-004 branch (`86b9cf24d6ff`) into one continuous chain. Before this merge, a full downgrade/re-upgrade walk that crosses *both* lineages in one continuous Alembic history was structurally impossible, so an existing incompatibility between them was unreachable and undetected. It is now reachable: four pre-existing downgrade-guard tests (`test_recall_downgrade_guard.py::test_clean_downgrade_with_no_recall_history_reupgrade_restores_exact_prior_state`, `test_migrations.py::test_migration_backfill_matches_pre_existing_lot_and_survives_downgrade_reupgrade`, `test_migrations.py::test_migration_upgrade_preserves_legacy_germination_check_rows`, `test_nursery_ops_downgrade_guard.py::test_migration_upgrade_blocked_by_pre_existing_mixed_seed_lot_lines`) downgrade the schema to a point that predates STORE-INV-004's `seed_lots.inventory_lot_id` column, then call the current, head-shaped `sowing_service.register_seed_lot` (whose INSERT unconditionally includes that column) against that older schema, and fail with `psycopg.errors.UndefinedColumn`. Confirmed structural (reproduces on a freshly reset, data-clean `cmp_test`), not a data-leakage artifact. Deciding how to reconcile this — e.g. making the column write conditional on schema state, giving these test helpers a raw-SQL seed-lot insert path for pre-STORE-INV-004 schema states, or something else — is a STORE-INV-004/NURSERY-OPS/CMP-020 test-fixture ownership decision, not a `VINES-OPS-001B` (Grow Cube/Grow Bag business logic) change, and is left unresolved here rather than silently patched. `VINES-OPS-001B`'s own new migrations (`475ed950cb4e`, `810e24e6ce43`, `43246a360bbd`) upgrade and downgrade cleanly on both legs of the merge; they are not implicated in the failure.
- ~~**`test_nursery_ops_downgrade_guard.py::test_migration_downgrade_blocked_when_seeding_provenance_exists` self-deadlocks against `cmp_test`.**~~ Resolved by PILOT-BLOCKER-007: root cause was exactly as identified during PILOT-BLOCKER-006 (see prior entry text in history) — the test's own scenario-building `Session`/`Connection` was never committed or closed before the same test invoked `command.downgrade(_cfg(), _PRE_NURSERY_OPS_REVISION)` in-process, so Postgres blocked the Planning migration's (`2c3b8d0bab94`) `DROP INDEX ix_sowing_events_seeding_program_line` (`ACCESS EXCLUSIVE` on `sowing_events`) on that connection's own still-open, uncommitted transaction against the same table — a lock it could only release in `finally: conn.close()`, which never ran because the thread was blocked inside `command.downgrade()` itself. Fixed by adding an explicit phase boundary in both affected tests in that file (`test_migration_downgrade_blocked_when_seeding_provenance_exists` and `test_migration_downgrade_blocked_when_sown_site_count_unrecorded`, which shared the identical pattern): `session.commit()` + `session.close()` + `conn.close()` immediately after the scenario's historical state is built and before the `command.downgrade(...)` call, mirroring how `test_migration_upgrade_blocked_by_pre_existing_mixed_seed_lot_lines` (same file) and `test_recall_downgrade_guard.py` already separate committed setup from the migration-command call. No Alembic migration, model, service, or route changed — tests only. Verified: the previously-hanging test passes alone (~25s), and the whole file (`pytest tests/test_nursery_ops_downgrade_guard.py -q`) now completes normally (3 passed, ~60s, no hang); `cmp_test` ends at the single dynamically-resolved Alembic head. **Sibling note (not fixed here, flagged for owner decision):** `test_batch_derivation_downgrade_guard.py::test_migration_downgrade_blocked_when_split_history_exists` has the same uncommitted-connection-before-`command.downgrade()` shape (writes to `sowing_events`/`batch_carrier_assignments` via `sowing_service.sow_batch` without committing before its own downgrade call) and is a latent risk of the identical self-deadlock; left out of PILOT-BLOCKER-007's scope since it was not the ticketed failing test and is not caused by a shared helper (each file inlines its own scenario-building code), per that ticket's explicit scope-control instructions. Do not run `pytest tests/test_nursery_ops_downgrade_guard.py` under any ad-hoc lock-timeout workaround going forward — the normal command above is now safe as-is.

## Operator workflow simplification decisions (PILOT-UX-006)

- **Quality "Urgent Hold" has no backend concept to surface.** The ticket asks that "Urgent Hold must remain clearly accessible" in the simplified Quality decision queue, but a repo-wide search (frontend and backend) finds no `urgent_hold`/`UrgentHold`/"Urgent Hold" concept anywhere — only an ordinary Hold disposition, offered with the same visual weight as Release/Reject (`apps/web/app/farms/[farmId]/store-inventory/quality/page.tsx`). Whether "Urgent Hold" is meant as (a) a pure UI fast-path/prominence treatment of the existing Hold action, or (b) a distinct domain disposition with its own sequencing/SoD rules, is a quality-release policy decision the ticket itself says Claude may not make ("Do not decide whether Hold should globally map to Caution vs Critical in this ticket"). Not implemented — inventing either the UI affordance or the underlying domain semantics without that decision would be exactly the kind of invented rule `CLAUDE.md` forbids.
- **Vines Production has no per-Grow-Bag assignment id to carry into Observations.** Leafy Production's "Record observation" link now carries the specific `batch_carrier_assignment_id` (Plate) into Observations, which pre-selects that exact target (`RecordObservationForm`'s new `initialTargetId` prop). Vines Production's placement read (`VinesProductionPlacementRead`) is deliberately aggregated one row per (Batch, Grow Gutter) — "never one row per plant/Grow Bag" per its own schema doc-comment — so no single assignment id exists at that row's granularity to carry forward truthfully; a Gutter can span several Grow Bag populations. Carrying forward only Batch context (already implemented, unchanged) is the truthful option; fabricating or guessing a single Grow Bag assignment id from the aggregated row was not done. A finer-grained Vines Production read (one row per Grow Bag/population) would be needed to close this gap for Vines specifically.
- **Nursery Journey branch-narrowing needs Batch/crop-workflow context this component doesn't have.** The ticket asks that once "context tells us the workflow," the Seedling→InterSalads/InterVines branch indicator show only the applicable branch instead of both joined by "or". `NurseryJourney` is deliberately data-free ("has no data dependency," per its own doc-comment) and is rendered identically on every Nursery list page (Germination/Seedling/InterSalads/InterVines/Sowing), none of which currently select a single Batch before rendering it — they are farm-wide worklists, not single-Batch detail views. Determining "the applicable branch" requires resolving a specific Batch's crop workflow configuration (which of InterSalads/InterVines that crop's version actually uses), a read this ticket's pages don't fetch and that isn't itself already exposed in a form these pages could use. The existing "or"-grouped, non-sequential presentation (already correct per this ticket's core complaint about false sequential arrows) is left as-is; true single-branch narrowing is deferred pending that workflow-configuration read.

## QR labels and scan context decisions (PILOT-SCAN-001)

- **Dynamic per-entity-type authorization on the generic QR routes.** Every other tenant-scoped route in this codebase is gated by exactly one static `Permission` via `Depends(require_permission(...))`, mechanically enforced by `test_authz_read_enforcement_architecture.py`/`test_authz_mutation_enforcement_architecture.py`. A single generic `GET /qr/{token}` resolver cannot fit that shape by construction: the token alone does not reveal which of the eight supported entity types (and therefore which `Permission`) applies until the row is read, and `POST /farms/{farm_id}/qr/{entity_type}/{entity_id}/generate`/`POST /qr/{token}/print` have the identical problem (`entity_type` is a wildcard path segment, not one of eight literal routes). Implemented as: `require_tenant_context` on every route (real authentication + active membership), then the exact per-entity-type `Permission` (`app/api/qr.py::ENTITY_PERMISSIONS`, a FROZEN mapping verified against the live route table for every one of the eight types — see `docs/domain/QR_SCAN_MODEL.md`) enforced via `has_permission()` inside the route body before any content is returned or any write happens — backend-enforced per CLAUDE.md rule 11, just one layer later than the mechanical per-route check reaches. Registered as a reviewable exemption (with justification) in both architecture tests, plus a compact parametrized behavioral proof (`tests/test_qr_authz.py`) that the dynamic gate holds for all eight entity types using the genuine existing role policy (never weakened to make the test pass). The underlying architectural gap (this codebase has no structural, test-enforced way to express "permission depends on runtime-resolved data" the way it does for a fixed `Permission`) remains real and is still flagged for review: should a future ticket introduce a declarative per-entity-type permission map the architecture tests themselves recognize, rather than an exemption comment? PILOT-SCAN-001's own closure did NOT invent such a mechanism — that would have been exactly the kind of unapproved architectural decision CLAUDE.md reserves for explicit review.
- ~~**`batch_carrier_assignment` (Batch Placement) reuses the Crop Batch permission tier.**~~ Corrected at final closure: `batch_carrier_assignment` is gated by `sowing.read`/`sowing.manage`, not `crop_batch.read`/`crop_batch.manage` — CMP-006's own BatchCarrierAssignment read/write authority already lives under Sowing (`sowing_service.list_batch_carriers`/`get_carrier_batch_assignment`, the exact two functions `qr_service` calls for this entity type, are `sowing.read`-gated in `app/api/sowings.py`), a precise existing permission, never a reused/broader one. The initial draft's `CROP_BATCH_READ`/`CROP_BATCH_MANAGE` choice was a plausible-sounding guess, not verified against the live route table, and would have incorrectly denied `operator` (has `sowing.manage` but not `crop_batch.manage`) the ability to print/generate a placement label for work it actually performs. `finished_goods_lot` had the identical class of bug (`finished_goods_storage.read`/`.manage` instead of the correct `packing.read`/`.manage`, `packing_service.get_finished_goods_lot`'s own real gate) and was corrected the same way. See `docs/domain/QR_SCAN_MODEL.md`'s permission table for the full, verified mapping.
- **`qr_identifiers.farm_id` is NOT NULL, not nullable-for-tenant-wide-entities as the ticket's conceptual model describes.** All eight pilot-supported entity types are already farm-scoped in the current domain (no tenant-wide scannable entity type exists yet), so a nullable `farm_id` column would be unused, speculative schema (CLAUDE.md: no code/schema for hypothetical future requirements). If a future ticket adds a genuinely tenant-wide scannable entity (e.g. `CarrierSpecification`, which is tenant-wide today), that migration should widen this column then.
- **Traceability deep-linking is limited to the four entry points the existing `/farms/{farmId}/traceability` page already supports.** That page's `?entryType=&id=` query-param initialization (added by this ticket) covers Crop Batch, Harvested Produce Lot, Graded Produce Lot, and Finished Goods Lot — the same four tabs the page already exposes. A `batch_carrier_assignment` (placement) scan's "View traceability" action therefore is not offered (no placement-level traceability tab exists to deep-link into); Location/Carrier/Asset scans likewise have no traceability action, matching the existing page's own scope. Not a QR-specific gap — the traceability page itself would need a fifth entry point to close this.
- **No combined "Recent activity" aggregation view for Batch/Location scans.** The ticket asks scan context to "expose a concise Recent activity/History route using existing authoritative records where practical," explicitly forbidding a second generic activity ledger. No combined cross-source (movements + observations + harvests + Work Items) aggregation endpoint exists in the current backend, and building one is a materially larger read than this ticket's own scope. The scan page instead surfaces each entity's own current facts (occupancy, placements, stage) plus matching Work Items directly, with "View Batch"/"View traceability" action links to the existing detail/traceability surfaces for deeper history — never a fabricated aggregation. Flagging the aggregation gap for a future ticket, as instructed.

## Operational stage labels and label-only printing decisions (PILOT-SCAN-001B)

- ~~**Every Operational Placement Label uses the Carrier's own QR entity type.**~~ Corrected at FINAL CLOSURE: this was the wrong call. A reusable Carrier can later hold a different Batch, so a placement label printed against the Carrier's own permanent QR would silently start resolving that NEW occupant once the physical object is reused — never the pilot's intent for a Batch-placement label. The QR now identifies the strongest existing stable Batch-placement identity each stage's own authoritative result/read model provides (`entity_type = "batch_carrier_assignment"`), which is available directly from the triggering command's own response for six of the seven stages (Sowing, Seedling, InterSalads, Leafy Production, InterVines, Vines Production). Germination placement is the one genuine exception: `TrayPlacementRead` (the `place_tray` command's own response) carries no `batch_carrier_assignment_id` — but the Germination worklist read the page already has loaded (`GerminationTrayRead.batch_carrier_assignment_id`) does, for that exact tray, and that assignment id is unchanged by the placement Movement (a Movement changes location, never the underlying placement identity). Looking it up there is using an already-authoritative value, never inventing one. See `docs/domain/QR_SCAN_MODEL.md`'s per-stage table for the full, verified breakdown, and `qr_service.py`'s `batch_carrier_assignment` resolve branch (unchanged since PILOT-SCAN-001) for why this QR entity type structurally cannot "become" a different Batch's identity the way a Carrier QR would.
- ~~**Germination's Carrier-QR fallback, for when the worklist lookup fails.**~~ Removed at a further FINAL CLOSURE pass: a bounded fallback to `entity_type = "carrier"` was briefly introduced for the one case where the worklist lookup above finds no matching row, reasoned as "better than no label at all." On review this reintroduced the exact ambiguity the whole entity-selection rule exists to prevent — presenting a Carrier QR AS this placement's label, when that same Carrier QR would later resolve a completely different Batch's occupancy once the physical tray is reused. There is now no implicit fallback between the three label identity meanings (Batch Master → `crop_batch`, Permanent Carrier → `carrier`, Operational Placement/Stage → `batch_carrier_assignment`). When the lookup fails, `GerminationReceiptCard` generates no Operational Placement Label at all (no QR of any entity type, no print button) and shows "Placement identity is not available yet. Refresh before printing." with a Refresh action against the existing worklist query. A separate, explicitly-labelled "Print Carrier Label instead" link is still offered in that state — a genuinely different, clearly-distinct label type (the Carrier's own permanent identity), never presented as a substitute for the placement label.
- **Vines Production placement label has no Greenhouse/Zone/Span breakdown and no per-Grow-Bag source-cube link.** `VinesProductionTransferRead`/`VinesProductionGrowBagPlacementRead` expose only a bare destination Grow Gutter id (resolved to a gutter code string by the page) and, per Grow Bag, only its own code and assigned plant count — no Zone/Span/Greenhouse chain, and no field linking a specific Grow Bag back to the specific source Grow Cube(s) it came from. The Vines Production placement label (`vinesProductionPlacementLabel`) uses exactly what is authoritatively available (Batch, Grow Bag code, Gutter code, plant count); it does not walk the Location tree to reconstruct a Zone/Span breadcrumb (that would require a separate Location-path read this ticket did not add) and does not guess a source Grow Cube. Closing this would need `VinesProductionTransferRead`'s own schema extended with a resolved Location path and a per-bag source-cube reference — a backend change, left for a follow-up ticket rather than invented here.
- ~~**Reprinting a Stage Label reuses PILOT-SCAN-001's existing generic label route, rather than a new per-stage reprint UI.**~~ Refined at FINAL CLOSURE into "Reprint Current Label": explicit entry points were added on the three current-context listing screens that already exist and already carry a `batch_carrier_assignment_id` per row (the Germination worklist, the Seedling workspace, Leafy Production's Active Production Plates table), each linking to the same existing generic `/farms/[farmId]/labels/batch_carrier_assignment/[assignmentId]` route (still the one reprint mechanism, unchanged), which was itself enriched (`lib/labels/labelContent.ts`) to also show the current physical carrier code and current location, not just Batch/crop/variety — a genuinely useful current-stage snapshot rather than a bare "Placement" label. **No entry point exists for InterSalads** — its own page is transaction-composition only, with no current-occupancy listing screen to hang a link on, and building one is out of this restrained closure ticket's scope; a future ticket adding a "currently in InterSalads" view (mirroring InterVines' own existing one) would close this. Vines Production/InterVines were left without this link too (not required to close the ticket's three issues, though their own existing placement tables could grow one cheaply later).

## Permanent physical QR and dynamic batch assignment decisions (PILOT-SCAN-001D)

- **Automatic creation-time QR provisioning made ~55 pre-existing migration/downgrade-guard tests fail, and they were deliberately left unfixed here.** Wiring `qr_provisioning.ensure_qr_identifier_for_new_entity` into `carrier_service.register_carrier`/`bulk_register_carriers`, `location_service.create_location`/`bulk_generate_children`, and `asset_service.register_asset` means every test's own incidental Carrier/Location/Asset creation now also creates a `qr_identifiers` row as a side effect — including inside the roughly dozen migration/downgrade-guard test files (name-matched on "migration"/"downgrade_guard") that exercise `29d6697de6d5` (PILOT-SCAN-001's own `qr_identifiers`-adding migration) and its own pre-existing downgrade guard ("Cannot downgrade past PILOT-SCAN-001's qr_identifiers table: N qr_identifiers row(s) already exist"). That guard now fires far more often, on scenario-building code that was never about QR at all, masking each such test's own intended assertion. Confirmed structural (reproduces on a freshly reset, data-clean `cmp_test`), not dirty-state noise. The 6 failures squarely inside QR-identifier test scope (`test_qr_identifier_migration.py`) were fixed here by rewriting that file's own scenario-building fixtures to construct `Carrier` rows via direct ORM insert, bypassing the now-auto-provisioning service layer. The remaining ~55 failures, all in migration/downgrade-guard files with no QR-specific business logic, were left unfixed: fixing them means systematically converting each affected file's own scenario-building helpers to raw-SQL/ORM entity construction (the same fix pattern already applied here, and already precedented elsewhere in this codebase for identical schema-evolution fragility — see "Migration graph decisions" above), which is a mechanical but wide-reaching sweep across many files unrelated to this ticket's own Carrier/Asset/Location/Batch identity scope. Per `CLAUDE.md`'s "scope exceeds the ticket" stop condition and this ticket's own "No full suites" instruction, this is recorded here as a known, real, out-of-scope consequence for a dedicated follow-up ticket rather than silently patched or silently left unmentioned.
- **Proof 9 (Carrier reused across Batches — old assignment QR still resolves the prior Batch historically) is established by code inspection, not a new live integration test.** `qr_service.py`'s `batch_carrier_assignment` resolve branch fetches a `BatchCarrierAssignment` row directly by its own primary key/entity id — unchanged by this ticket — so an ended assignment's QR necessarily continues resolving that exact historical (Batch, Carrier, period) triple regardless of whatever new assignment the same Carrier is later given; there is no code path by which resolving an old placement QR could instead return a different, newer assignment. Building a full live HTTP scenario that legitimately releases a Carrier from Batch A's assignment and then reassigns the same Carrier to Batch B, through only already-tested domain commands, was judged materially more expensive than the direct-fetch-by-PK proof above already provides for the same claim, given this ticket's own "focused tests only" instruction. Flagged here rather than silently assumed.
- **Automatic QR provisioning applies uniformly to every Location record, including purely structural, non-occupiable ones (e.g. Zone, Span).** The ticket's PERMANENT QR RULE names "LOCATION" without carving out an occupiable-leaf-only subset, and CLAUDE.md's generic-location model treats the whole chain (Zone → Span → grow table, etc.) as Location records of equal standing — there is no existing "leaf vs. structural" distinction in the domain model to hang a narrower rule on. Provisioning a permanent QR for every Location the same way, rather than inventing an occupiable/structural split, keeps the rule simple, matches "every permanent physical Location receives its QR identity automatically at creation" literally, and costs nothing (a QR identifier row is cheap and a Zone/Span could plausibly be scanned to identify a physical position in the field even though nothing occupies it directly). Not treated as a gap — recorded for visibility since a narrower interpretation was possible.
