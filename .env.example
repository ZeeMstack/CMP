# Local development only — never commit a real .env.
# Values here must stay consistent with apps/api/.env.example (DATABASE_URL
# embeds the same user/password/db/port as the POSTGRES_* variables below).
POSTGRES_USER=cmp
POSTGRES_PASSWORD=cmp
POSTGRES_DB=cmp
POSTGRES_PORT=5432
